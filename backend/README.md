Backend (Node.js + Express + Socket.io + MongoDB)

Instructions:
1. Install dependencies:
   cd backend
   npm install

2. (Optional) Start a local MongoDB instance or set MONGO_URI env var:
   export MONGO_URI="mongodb://localhost:27017/whatsapp_clone"

3. Start the server:
   npm start

The server will listen on port 5000 by default.
